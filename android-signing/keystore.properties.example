# Local signing config for release builds. Do NOT commit this file.
# Copy to android/keystore.properties after `npx cap add android`,
# then fill in the values for your keystore.

storeFile=../studyflow-release.jks
storePassword=CHANGE_ME
keyAlias=studyflow
keyPassword=CHANGE_ME
